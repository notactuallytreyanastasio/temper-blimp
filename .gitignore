# Build output
build/
# ...except these, which are test *inputs* that happen to be named build/
!temper/cli/src/test/resources/build/
!temper/functional-test-suite/src/commonMain/resources/types/string/build/
.gradle/
zig-out/
.zig-cache/
temper.out/
temper.keep/
target/
node_modules/

# Local tooling
.DS_Store

# PR bodies, used with gh pr create --body-file
.prs/
